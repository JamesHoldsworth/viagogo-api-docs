title: AddressesLinks
type: object
properties:
  self:
    nullable: true
    oneOf:
      - $ref: ./Link.yaml
  'address:create':
    description: Add a new address for the current user.
    nullable: true
    oneOf:
      - $ref: ./Link.yaml
  first:
    nullable: true
    oneOf:
      - $ref: ./Link.yaml
  last:
    nullable: true
    oneOf:
      - $ref: ./Link.yaml
  next:
    nullable: true
    oneOf:
      - $ref: ./Link.yaml
  prev:
    nullable: true
    oneOf:
      - $ref: ./Link.yaml
